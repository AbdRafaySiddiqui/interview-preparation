<?php
$person = array("name" => "John", "age" => 30, "city" => "New York");

// Use a loop to print each key-value pair in the associative array.
foreach ($person as $key => $value) {
    echo "$key: $value<br>";
}
?>
